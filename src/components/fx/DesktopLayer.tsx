import { CustomCursor } from './CustomCursor';

/**
 * Всё, что грузится только для десктопа с мышью. Отдельный модуль нужен именно
 * ради этого: Vite выносит его в свой чанк, и на телефон он не приезжает.
 *
 * Жидкое искажение живёт не здесь, а внутри первого экрана: оно осмысленно только
 * там, где есть что искажать, и держать ради него постоянный полноэкранный
 * WebGL-контекст на каждой странице незачем.
 */
const DesktopLayer = () => <CustomCursor />;

export default DesktopLayer;
